import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';
import 'src/app.dart';

void main () {
  // Helper for rendering edges of elements in screen. Useful when you need to check the boundaries of the children elements of a parent element.
  // debugPaintSizeEnabled = true;

  runApp(App());
}